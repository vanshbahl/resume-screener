# Single source of truth for parser and schema versions.
# Import PARSER_VERSION from here wherever it is needed — never hardcode the string.

PARSER_VERSION: str = "2.3.0"
SCHEMA_VERSION: str = "1.2.0"
